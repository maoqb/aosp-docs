# 智能指针与所有权

当天目标：看到智能指针时能立即判断对象共享方式与潜在生命周期问题。

- `unique_ptr` 表达唯一所有者，使用移动转交所有权。
- `shared_ptr` 适合真正的共享所有权，但不要用它替代设计。
- `weak_ptr` 解决 `shared_ptr` 循环引用，使用前必须检查是否还能提升。
- 原始指针和引用常是非拥有借用；调用方仍须保证目标存活。

练习：用两个 `shared_ptr` 制造循环引用，再把其中一个改为 `weak_ptr`。
