# 错误处理与调试工具

当天目标：能定位 Native 崩溃，并理解 Android C++ 为什么常返回状态码。

- Native 层常用状态码或 `Result` 返回失败，而不是依赖 C++ 异常。
- 区分可恢复错误、程序错误（assert）和系统错误（`errno`）。
- AddressSanitizer 能定位越界、use-after-free 等内存问题。
- UndefinedBehaviorSanitizer 能帮助发现未定义行为。

练习：制造一次数组越界和一次悬空指针访问，用 Sanitizer 阅读报告的栈。
