# 类、继承、虚函数与接口

当天目标：读懂 Android 服务接口和实现类之间的多态关系。

- 基类析构函数通常应为 `virtual`，否则经基类指针销毁派生类会出错。
- 纯虚函数定义接口；`override` 让编译器检查重写签名。
- `final` 禁止继续继承或重写，表达设计限制。
- 看到虚函数调用时，要找实际对象的动态类型，而不是只看静态指针类型。

练习：定义抽象 `Renderer` 接口和两个实现，使用基类引用调用不同实现。
