# 拷贝、移动与值语义

当天目标：理解为什么有些 AOSP 类型不可拷贝，以及 `std::move` 实际做了什么。

- 拷贝构造/赋值会复制资源；资源独占类型通常应禁用拷贝。
- 移动构造/赋值转移资源控制权，移动后的对象仍有效但状态未指定。
- `std::move` 只把表达式转成右值，不会自行移动任何资源。
- 优先遵守 Rule of Zero：让成员类型管理资源，自己不手写特殊成员函数。

练习：实现一个持有字符串的类，打印拷贝和移动时机，再放进 `std::vector` 观察行为。
