# JNI 边界

当天目标：从 Java `native` 方法追到 C++，并避免 JNI 引用和线程错误。

- `JNIEnv*` 只在当前线程有效，不能跨线程缓存使用。
- 区分局部引用、全局引用与弱全局引用；循环调用时要及时删除局部引用。
- Native 创建线程后，需 attach 到 JVM 才能使用 JNI。
- JNI 调用后检查 Java 异常；有异常时不要继续做普通 JNI 调用。

练习：写一个 Java 调用 Native 字符串处理的例子，分别返回字符串和抛出异常。
