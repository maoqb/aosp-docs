# 模板、Lambda 与回调

当天目标：能读懂常用模板代码和异步回调，不陷入模板细节。

- 模板让类型在编译期参数化；先看调用点推断 `T` 是什么。
- Lambda 的 `[=]`、`[&]`、`[this]` 捕获决定对象是否复制或借用。
- 异步回调中引用捕获最危险：回调运行时，被引用对象可能已销毁。
- `std::function` 是通用回调容器，可能有额外分配和类型擦除开销。

练习：写一个异步任务队列，分别测试值捕获和引用捕获的生命周期差异。
