# 线程、锁与原子变量

当天目标：阅读线程安全代码时能找出共享状态、锁边界和潜在竞态。

- `std::mutex` 保护共享状态；使用 `lock_guard` 或 `unique_lock` 管理加锁范围。
- 条件变量必须配合谓词检查，避免虚假唤醒。
- 原子变量适合简单状态或计数，不等同于给复杂对象自动加锁。
- 避免持锁调用外部代码、Binder 调用或回调，防止死锁和长时间阻塞。

练习：实现生产者/消费者队列，并为退出信号设计正确的条件变量谓词。
