# RAII 与资源管理

当天目标：理解为什么 Android Native 代码常把锁、文件描述符和内存封装成对象。

- RAII：资源在构造时获取、在析构时释放，异常或提前返回也不会遗漏释放。
- `std::lock_guard` 会在离开作用域时解锁，避免手写 `unlock`。
- 文件描述符、Binder 引用和 mmap 内存都需要明确的释放责任。
- 看到析构函数时，先找它释放了什么资源。

练习：写一个只负责关闭 fd 的小类，验证多条 `return` 路径都能关闭文件。
