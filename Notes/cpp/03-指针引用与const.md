# 指针、引用与 const

当天目标：阅读函数签名时能判断参数可否为空、是否修改对象、是否转移所有权。

- `T*` 常表示可空的借用；先找出它的生命周期由谁保证。
- `T&` 表示必须存在的借用；`const T&` 不允许通过该引用修改对象。
- `const T*`、`T* const`、`const T* const` 的 const 位置不同。
- 不用裸指针表达拥有关系；拥有关系优先用 `unique_ptr` 或 Android 的 `sp<>`。

练习：为同一个函数设计传值、`const&`、指针三个版本，并写下各自适用条件。
