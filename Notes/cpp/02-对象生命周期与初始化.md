# 对象生命周期与初始化

当天目标：能说清对象何时构造、何时析构，以及栈对象和堆对象的区别。

- 栈对象在作用域结束时自动析构；这比 Java 的 GC 更确定。
- `new` 创建的对象必须有明确的释放策略，优先交给智能指针。
- 优先使用初始化列表初始化成员，而不是构造后再赋值。
- 成员按声明顺序初始化，不按初始化列表书写顺序初始化。

练习：为构造函数和析构函数打印日志，比较局部对象、`new` 对象和容器中对象的生命周期。
